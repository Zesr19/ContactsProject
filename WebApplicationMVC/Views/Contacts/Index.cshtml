@using DomainLayer.Models;
@model IEnumerable<ContactsModel>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

@foreach (var contactos in Model)
{
    <p>@contactos.Id, @contactos.Name, @contactos.Nickname, @contactos.Email, @contactos.Telephone</p>
}